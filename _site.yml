name: uataq
include:
  - favicon.ico
  - _data.rds
output:
  html_document:
    css: styles.css
    includes:
      in_header: _includes/head.html
    lib_dir: site_libs
    self_contained: no
output_dir: _site
